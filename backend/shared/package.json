{
  "name": "shared",
  "version": "1.0.0",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "dev": "nodemon -e ts -w . -x ts-node ./src/index",
    "build": "tsc",
    "test": "jest"
  },
  "setupFiles": [
    "./jest-setup-file.ts"
  ],
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  },
  "dependencies": {}
}
